      {/* Select para Escolher Exchange */}
      <div className="flex flex-col items-center">
        <span className="text-sm text-gray-400 mb-1">Selecionar Exchange</span>
        <select
          value={selectedExchange}
          onChange={(e) => setSelectedExchange(e.target.value)}
          className="bg-gray-700 text-white px-4 py-2 rounded-md w-full text-center cursor-pointer"
        >
          {/* Lista de Exchanges A (Spot) */}
          <optgroup label="Exchange A - Spot">
            {data.spots.map((spot, index) => (
              <option key={index} value={spot.exchange}>
                {spot.exchange} - Bid: {spot.bid.toFixed(6)}
              </option>
            ))}
          </optgroup>

          {/* Lista de Exchanges B (Futuros) */}
          <optgroup label="Exchange B - Futuros">
            {data.futures.map((future, index) => (
              <option key={index} value={future.exchange}>
                {future.exchange} - Ask: {future.ask.toFixed(6)}
              </option>
            ))}
          </optgroup>
        </select>
      </div>